import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    mainContainer: {
      position: 'relative',
      top: 0,
      left: 0,
      padding: 0,
      margin: 0
    },
  });